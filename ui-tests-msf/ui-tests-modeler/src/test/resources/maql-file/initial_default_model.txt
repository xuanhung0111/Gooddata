CREATE DATASET {dataset.company} VISUAL(TITLE "company", DESCRIPTION "company");
CREATE DATASET {dataset.city} VISUAL(TITLE "city", DESCRIPTION "city");
CREATE FOLDER {dim.company} VISUAL(TITLE "Company") TYPE ATTRIBUTE;
CREATE ATTRIBUTE {attr.company.security} VISUAL(TITLE "security", DESCRIPTION "security", FOLDER {dim.company}) AS KEYS {f_company.id} FULLSET;
ALTER DATASET {dataset.company} ADD {attr.company.security};
ALTER ATTRIBUTE {attr.company.security} ADD LABELS {label.company.security} VISUAL(TITLE "security") AS {f_company.nm_security};
ALTER ATTRIBUTE {attr.company.security} DEFAULT LABEL {label.company.security};
CREATE ATTRIBUTE {attr.company.employee} VISUAL(TITLE "employee", DESCRIPTION "employee", FOLDER {dim.company}) AS KEYS {d_company_employee.id} FULLSET, {f_company.employee_id};
ALTER DATASET {dataset.company} ADD {attr.company.employee};
ALTER ATTRIBUTE {attr.company.employee} ADD LABELS {label.company.employee} VISUAL(TITLE "employee") AS {d_company_employee.nm_employee};
ALTER ATTRIBUTE {attr.company.employee} ADD LABELS {label.company.employee.worker} VISUAL(TITLE "worker") AS {d_company_employee.nm_worker};
ALTER ATTRIBUTE {attr.company.employee} ADD LABELS {label.company.employee.lazyworker} VISUAL(TITLE "lazyworker") AS {d_company_employee.nm_lazyworker};
ALTER ATTRIBUTE {attr.company.employee} ADD LABELS {label.company.employee.hardworker} VISUAL(TITLE "hardworker") AS {d_company_employee.nm_hardworker};
ALTER ATTRIBUTE {attr.company.employee} DEFAULT LABEL {label.company.employee};
CREATE FOLDER {dim.city} VISUAL(TITLE "City") TYPE ATTRIBUTE;
CREATE ATTRIBUTE {attr.city.accounter} VISUAL(TITLE "accounter", DESCRIPTION "accounter", FOLDER {dim.city}) AS KEYS {f_city.id} FULLSET;
ALTER DATASET {dataset.city} ADD {attr.city.accounter};
ALTER ATTRIBUTE {attr.city.accounter} ADD LABELS {label.city.accounter} VISUAL(TITLE "accounter") AS {f_city.nm_accounter};
ALTER ATTRIBUTE {attr.city.accounter} DEFAULT LABEL {label.city.accounter};
CREATE ATTRIBUTE {attr.city.people} VISUAL(TITLE "people", DESCRIPTION "people", FOLDER {dim.city}) AS KEYS {d_city_people.id} FULLSET, {f_city.people_id};
ALTER DATASET {dataset.city} ADD {attr.city.people}; ALTER ATTRIBUTE {attr.city.people} ADD LABELS {label.city.people} VISUAL(TITLE "people") AS {d_city_people.nm_people};
ALTER ATTRIBUTE {attr.city.people} DEFAULT LABEL {label.city.people};
CREATE FOLDER {ffld.company} VISUAL(TITLE "Company") TYPE FACT;
CREATE FACT {fact.company.age} VISUAL(TITLE "age", DESCRIPTION "age", FOLDER {ffld.company}) AS {f_company.f_age};
ALTER DATASET {dataset.company} ADD {fact.company.age};
CREATE FACT {fact.company.salary} VISUAL(TITLE "salary", DESCRIPTION "salary", FOLDER {ffld.company}) AS {f_company.f_salary};
ALTER DATASET {dataset.company} ADD {fact.company.salary};
CREATE FOLDER {ffld.city} VISUAL(TITLE "City") TYPE FACT;
CREATE FACT {fact.city.price} VISUAL(TITLE "price", DESCRIPTION "price", FOLDER {ffld.city}) AS {f_city.f_price};
ALTER DATASET {dataset.city} ADD {fact.city.price};
ALTER ATTRIBUTE {attr.company.security} ADD KEYS {f_city.company_id}; SYNCHRONIZE {dataset.company}, {dataset.city};