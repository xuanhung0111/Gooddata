CREATE DATASET {dataset.city} VISUAL(TITLE "city", DESCRIPTION "city"); CREATE FOLDER {dim.city} VISUAL(TITLE "City") TYPE ATTRIBUTE; CREATE ATTRIBUTE {attr.city.factsof} VISUAL(TITLE "Records of city", DESCRIPTION "Records of city", FOLDER {dim.city}) AS KEYS {f_city.id} FULLSET; ALTER DATASET {dataset.city} ADD {attr.city.factsof}; CREATE ATTRIBUTE {attr.city.employee} VISUAL(TITLE "employee", DESCRIPTION "employee", FOLDER {dim.city}) AS KEYS {d_city_employee.id} FULLSET, {f_city.employee_id}; ALTER DATASET {dataset.city} ADD {attr.city.employee}; ALTER ATTRIBUTE {attr.city.employee} ADD LABELS {label.city.employee} VISUAL(TITLE "employee") AS {d_city_employee.nm_employee}; ALTER ATTRIBUTE {attr.city.employee} DEFAULT LABEL {label.city.employee}; CREATE FOLDER {ffld.city} VISUAL(TITLE "City") TYPE FACT; CREATE FACT {fact.city.age} VISUAL(TITLE "age", DESCRIPTION "age", FOLDER {ffld.city}) AS {f_city.f_age}; ALTER DATASET {dataset.city} ADD {fact.city.age}; SYNCHRONIZE {dataset.city};