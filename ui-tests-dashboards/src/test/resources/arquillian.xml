<arquillian xmlns="http://jboss.org/schema/arquillian"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
        http://jboss.org/schema/arquillian
        http://jboss.org/schema/arquillian/arquillian_1_0.xsd">

	<!-- possible options are listed at https://docs.jboss.org/author/display/ARQ/Drone -->
	<extension qualifier="webdriver">
		<property name="browser">${browser:firefox}</property> <!-- use firefox directly if you want to run tests from IDE -->
		<property name="remoteReusable">false</property>
		<property name="remoteAddress">http://localhost:4444/wd/hub</property>
		<property name="chromeDriverBinary">/Applications/Google Chrome.app/Contents/MacOS/Google Chrome</property>
	</extension>

	<extension qualifier="graphene">
		<property name="waitModelInterval">${timeout:30}</property>
		<property name="waitGuiInterval">${timeout:30}</property>
		<property name="waitAjaxInterval">${timeout:30}</property>
		<property name="waitGuardInterval">${timeout:30}</property>
	</extension>
	
</arquillian>